package tree;
public class For_80_LPA_ka_Question {
    

    // if we have given a number n 
    // then n to the power (n-2) trees are possible with that n^(n-2)

    // tree is always a valid bipertite graph

    // if in a tree 
    // a is the node at even level and b is the node at odd level then 
    // all possible trees with that is a^(b-1) * b^(a-1)




}
