

public class sliding_window {

    public static void main(String[] args) {
        // int arr[]  = {1,3,5,7,4};
        // for(int i = 0; i<arr.length;i++){
        //     hm.put(i, arr[i]);
        // }
        // for(int i = 0;i<arr.length;i++){
        //     int sub = 6-arr[i];
        //     if(hm.get(i) != sub && hm.containsValue(sub)){
        //         hm.containsValue(sub);
        //     }
        // }

        String ss = "abcdefgh";
        String s2 = "abc";

        System.out.println(ss + s2);

        for(int i = 0;i<ss.length();i++){
            // System.out.println((ss.charAt(i)+1));
        }
    }
}